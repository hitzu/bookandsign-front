# Lightable-Nextjs
Lightable-Nextjs

## Supabase Storage (Fotos)

Para que `Fotos` cargue el contenido, actualiza el manifest embebido:

- `src/media/mediaManifest.ts`

Este archivo se genera con tu script (o se copia desde tu `media.json`) y contiene URLs públicas (no hay listado de buckets, ni keys).